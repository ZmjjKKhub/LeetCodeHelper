# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Copied from ../design/tokens.css by vite.config.ts's syncDesignTokens
# plugin on every dev/build; never hand-edited, so never committed. Regen
# by running `npm run dev` or `npm run build` again.
src/styles/tokens.generated.css

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
